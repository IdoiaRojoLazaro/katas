import {  } from "module";
document.addEventListener('DOMContentLoaded', () => {
	const gameContainer = document.getElementById('game-of-life-container');

	// Suponiendo que tienes un método para obtener la matriz actual de celdas
	const cellMatrix = GameOfLife.create(matrix).getCurrentMatrix();

	// Renderiza la matriz de celdas en el DOM
	// Debes escribir la lógica para convertir la matriz de celdas en elementos HTML
});
